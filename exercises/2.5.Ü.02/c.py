# Convert a float to an integer
# Note: int() drops the decimal part (truncates toward zero). It does NOT round.
float_var = 38.92
converted_int = int(float_var)
print(f"Converted number: {converted_int}") # 38